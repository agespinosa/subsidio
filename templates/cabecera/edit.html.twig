{% extends 'base.html.twig' %}

{% block title %}Edit Cabecera{% endblock %}

{% block body %}
    <h1>Edit Cabecera</h1>

    {{ include('cabecera/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('cabecera_index') }}">back to list</a>

    {{ include('cabecera/_delete_form.html.twig') }}
{% endblock %}
